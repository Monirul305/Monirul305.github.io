**/*.map
**/*.min.css
**/*.min.js
assets/css/main.scss
assets/js/distillpub/template.v2.js
assets/js/search/*.js
assets/plotly/demo.html
lighthouse_results/**
_posts/*.md
_sass/font-awesome/*.scss
_scripts/*
# Ignore citation YAML file generated by script
_data/citations.yml

# Heavy-lift drone sizing tool — delivered verbatim from a separate
# workstream (the optimizer-developer agent). The JS/CSS/MD are
# code-formatted by the generator; the JSON datasets are long machine-
# generated curves. Don't let prettier re-wrap any of them.
_pages/heavy-lift-drone-tool.md
assets/css/heavy-lift-drone-tool.css
assets/js/heavy-lift-drone-tool.js
assets/data/heavy-lift-drone-motors/*.json
assets/data/heavy-lift-drone-presets.json

# -----------------------------------------------------------------------------
# User-authored portfolio content (Md Monirul Islam)
# Prettier reformatting risks rewrapping long lines and re-flowing wide YAML
# in ways that would change the visual rendering. Skip these and keep prettier
# checks for files we don't author (theme code).
# -----------------------------------------------------------------------------
_pages/about.md
_pages/cv.md
_pages/projects.md
_pages/publications.md
_news/*.md
_projects/*.md
_bibliography/papers.bib
_data/socials.yml
assets/json/resume.json
_config.yml
.github/workflows/render-cv.yml
_layouts/post.liquid

# -----------------------------------------------------------------------------
# Upstream al-folio docs — we inherit these from the fork and don't author them.
# They don't currently pass prettier (an al-folio CI/main mismatch). They're
# also in _config.yml's exclude: list so they don't build into the site.
# -----------------------------------------------------------------------------
CUSTOMIZE.md
